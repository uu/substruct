<% if @order.downloads.count > 0 %>
  <%= content_tag :h2, t(:product_downloads) %>
  <p>
    <%= t(:product_downloads_info) %>
    <ul>
      <% @order.downloads.each do |dl| %>
        <li>
          <%= 
            link_to(
              dl.product.name,
              :controller => 'customers',
              :action => 'download_for_order',
              :params => {
                :order_number => @order.order_number,
                :download_id => dl.id
              }
            )
          %>
        </li>
      <% end %>
    </ul>
  </p>
<% end %>
