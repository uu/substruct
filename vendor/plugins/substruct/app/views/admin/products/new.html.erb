<%= form_tag({:action => 'save'}, :multipart => true) %>
<div id="left">
  <h1><%= @title %></h1>
  <ul class="navigation secondary">
    <%= content_tag :li, link_to(t(:back_to_product_list), :controller => 'products', :action => 'list') %>
  </ul>

    <%= render :partial => 'form' %>
    <%= submit_tag t(:save_new_product), :class => 'button hundredfifty' %>

</div>

<%= render(:partial => 'crud_right') %>
</form>
