<%= form_tag({:action => 'save', :id => @product}, :multipart => true) %>
<div id="left">
  <h1><%= @title %></h1>
  <ul class="navigation secondary">
    <%= content_tag :li, link_to(t(:back_to_product_list), :controller => 'products', :action => 'list') %>
  </ul>

    <%= render :partial => 'form' %>
		<div style="float:right;">
			<%= link_to t(:delete_this_product), {:action => 'destroy', :id => @product}, :confirm => t(:confirm), :class => 'action' %>
		</div>
    <%= submit_tag t(:save_this_product), :class => 'button hundredthirty' %>

</div>

<%= render(:partial => 'crud_right') %>

<!-- end form tag here to grab tags on products -->
</form>
